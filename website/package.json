{
  "name": "aegispass-website",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "scaffold:pages": "node scripts/scaffold-pages.mjs",
    "predev": "npm run scaffold:pages",
    "prebuild": "npm run scaffold:pages",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "vitest run"
  },
  "devDependencies": {
    "astro": "^5.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
