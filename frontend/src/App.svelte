<script lang="ts">
  // Componente raiz (esqueleto). A UI real do cofre chega nas tasks VAULT-*.
  const product = "AegisPass";
</script>

<main>
  <h1>{product}</h1>
  <p>Frontend (esqueleto). Cifragem Zero-Knowledge no cliente em <code>src/lib/crypto.ts</code>.</p>
</main>

<style>
  main {
    font-family: system-ui, sans-serif;
    max-width: 40rem;
    margin: 4rem auto;
    padding: 0 1rem;
  }
</style>
