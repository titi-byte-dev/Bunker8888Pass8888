{
  "name": "aegispass-site",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Site institucional publico (aegispass.com) — separado da app.",
  "scripts": {
    "sync:tokens": "node scripts/sync-tokens.mjs",
    "predev": "npm run sync:tokens",
    "prebuild": "npm run sync:tokens",
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.6",
    "@sveltejs/kit": "^2.63.0",
    "@sveltejs/vite-plugin-svelte": "^5.0.0",
    "@types/node": "^25.9.1",
    "svelte": "^5.0.0",
    "svelte-check": "^4.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
